// Source: Day05-FlinkSQL (pb9wqs8sv708mmpl) snippet #15
package com.bigdata.snippets;

class pb9wqs8sv708mmplSnippet015 {
    // Tutorial snippet — may require surrounding context to compile.
    public static void demo() throws Exception {
        /*
         * IntelliJ IDEA file template for Flink SQL jobs:
         *
         * #if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
         * #parse("File Header.java")
         * import org.apache.flink.streaming.api.environment.StreamExecutionEnvironment;
         * import org.apache.flink.table.api.bridge.java.StreamTableEnvironment;
         *
         * public class ${NAME} {
         *     public static void main(String[] args) throws Exception {
         *         StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
         *         env.setRuntimeMode(RuntimeExecutionMode.AUTOMATIC);
         *         StreamTableEnvironment tableEnv = StreamTableEnvironment.create(env);
         *         // 2. 创建表对象
         *         // 3. 编写sql语句
         *         // 4. 将Table变为stream流
         *         env.execute();
         *     }
         * }
         */
    }
}
