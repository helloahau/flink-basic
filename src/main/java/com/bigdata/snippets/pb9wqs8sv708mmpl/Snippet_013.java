// Source: Day05-FlinkSQL (pb9wqs8sv708mmpl) snippet #13
package com.bigdata.snippets;

class pb9wqs8sv708mmplSnippet013 {
    // Tutorial snippet — may require surrounding context to compile.
    public static void demo() throws Exception {
        /*
         * StreamTableEnvironment tableEnv = ...;
         * DataStream<Tuple2<Long, Integer>> stream = ...;
         *
         * // 将数据流转换成只包含f1字段的表
         * Table table = tableEnv.fromDataStream(stream, $("f1"));
         *
         * // 将数据流转换成包含f0和f1字段的表，在表中f0和f1位置交换
         * Table table = tableEnv.fromDataStream(stream, $("f1"), $("f0"));
         *
         * // 将f1字段命名为myInt，f0命名为myLong
         * Table table = tableEnv.fromDataStream(stream, $("f1").as("myInt"), $("f0").as("myLong"));
         */
    }
}
