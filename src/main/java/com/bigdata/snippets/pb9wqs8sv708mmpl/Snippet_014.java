// Source: Day05-FlinkSQL (pb9wqs8sv708mmpl) snippet #14
package com.bigdata.snippets;

class pb9wqs8sv708mmplSnippet014 {
    // Tutorial snippet — may require surrounding context to compile.
    public static void demo() throws Exception {
        /*
         * StreamTableEnvironment tableEnv = ...;
         * DataStream<Event> stream = ...;
         *
         * Table table = tableEnv.fromDataStream(stream);
         * Table table = tableEnv.fromDataStream(stream, $("user"));
         * Table table = tableEnv.fromDataStream(stream, $("user").as("myUser"), $("url").as("myUrl"));
         */
    }
}
